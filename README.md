# Lab 5 - Starter
Make sure you make a PR to your own repo's main and not the class' repo!! Otherwise you will lose points!!
Yirui(Ray) Wang

Q1: Would you use a unit test to test the "message" feature?
No, because message feature is not a single unit, so it is very hard to test it as a single function.
Q2: Would you use a unit test to test the "max message length" feature?
Yes, because this feature is independent and easy to test.